```
<ScrollList items={scrollData} viewportHeight={300} scrollTop={1500}>
  {item => (
    <div>
      <h3>{item.title}</h3>
      <p dangerouslySetInnerHTML={{ __html: item.body }} />
    </div>
  )}
</ScrollList>
```